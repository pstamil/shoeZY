import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    secondary: {
      main: "#7A73EA",
    },
    primary: {
      main: "#FF5A5F",
    },
    success: {
      main: "#00C194",
    },
  },
  typography: {
    fontFamily: "Poppins, sans-serif",
  },
});
